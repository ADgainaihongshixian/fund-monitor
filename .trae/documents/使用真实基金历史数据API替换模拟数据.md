## 实现方案

### 1. 分析当前代码结构
- 检查 `api.ts` 文件中的 `getFundHistory` 方法实现
- 了解现有的API调用方式、数据处理逻辑和错误处理机制
- 分析其他相关API方法的实现风格，确保代码一致性

### 2. 选择合适的基金历史数据API
- 评估新浪财经、天天基金网等平台的历史数据API
- 选择一个稳定可靠、数据格式清晰的API
- 验证API的访问限制和使用条款

### 3. 修改 `getFundHistory` 方法
- 替换模拟数据生成逻辑，使用真实API获取历史数据
- 调整URL构建逻辑，确保正确的API端点和参数
- 添加必要的请求头信息，避免403 Forbidden错误
- 实现正确的数据解析逻辑，匹配真实API返回的数据格式

### 4. 增强错误处理机制
- 处理API返回的各种错误状态码
- 处理网络连接失败、超时等异常情况
- 提供详细的错误信息，便于调试和用户提示

### 5. 验证修改后的代码
- 启动开发服务器，测试API调用是否成功
- 检查浏览器预览，确保历史数据走势图能够正确渲染
- 验证不同基金代码和时间范围的数据获取是否正常
- 测试错误处理机制，确保在API调用失败时能够显示友好的错误提示

### 6. 优化性能和用户体验
- 添加数据缓存机制，减少重复请求
- 优化数据解析和处理逻辑，提高性能
- 确保图表渲染流畅，特别是在处理大量数据时

## 预期结果
- 成功替换模拟数据，使用真实的基金历史数据API
- 历史数据走势图能够正确渲染，反映真实的历史数据变化趋势
- 错误处理机制完善，能够处理各种异常情况并提供友好提示
- 代码结构清晰，与现有代码风格一致
- 性能和用户体验良好，满足项目需求