## 修复方案

### 1. 分析API错误
- 检查错误信息："No route providing a controller name was found to match request URI"
- 确认当前使用的API端点 `/FundHistory/GetHistoryNetValue` 不存在
- 验证API请求的完整URL和参数格式

### 2. 寻找正确的东方财富网历史数据API端点
- 搜索东方财富网的历史数据API文档或相关资源
- 验证其他可能的API端点路径
- 确保API端点的正确性和可用性

### 3. 修改 `getFundHistory` 方法
- 更新API端点路径，使用正确的东方财富网历史数据API
- 调整请求参数，确保与API要求一致
- 保留现有的错误处理机制和缓存逻辑

### 4. 验证修改后的代码
- 启动开发服务器，测试API调用是否成功
- 检查浏览器预览，确保历史数据走势图能够正确渲染
- 验证不同基金代码和时间范围的数据获取是否正常
- 测试错误处理机制，确保在API调用失败时能够显示友好的错误提示

### 5. 优化性能和用户体验
- 确保数据缓存机制仍然有效
- 优化数据解析和处理逻辑
- 确保图表渲染流畅，特别是在处理大量数据时